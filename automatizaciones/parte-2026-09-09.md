[PARA CEREBRO] Secuencia Radiografía 3-7 construida y en la rama; NO está viva en n8n — no tengo URL ni API key de la instancia de Maikel, así que "activo en modo prueba" queda a un import de distancia. Lista única de lo que necesito abajo. Riesgo de duplicado con Landing a deslindar antes de producción.

# Parte de turno · Automatización · 2026-09-09

Recibido el encargo del timbre de las 15:32 y ejecutado en el mismo turno.

## Entregado hoy (`automatizaciones/radiografia/`)

1. **`workflow-secuencia.json`** (29 nodos): webhook de entrada (lo llama el workflow GHL de Landing al etiquetar `diagnostico-completo`/`radiografia-anonima`) → día 1 email SOLO si no dejó WhatsApp → día 3 y día 7 emails por cuello (copies del recorrido §2, los 6 cuellos incluido "ninguno") → día 7+24h sin respuesta: etapa **Tibio** + tarea SDR a 30 días. Sin día 14. Antes de cada envío relee el contacto: etiqueta `secuencia-parada` = muerte inmediata de la secuencia, aunque llegue en mitad de una espera.
2. **`workflow-parada.json`** (8 nodos): webhook único de parada para email reply, WhatsApp reply y cita. Etiqueta + nota con la respuesta literal + **tarea PUENTE a Maikel con vencimiento 2h** (mini-brief, guía de reacción por cuello y el mensaje de las dos salidas ya redactado). Si es cita, solo etiqueta+nota. Outbound y Landing solo tienen que hacer un POST — contrato de payload en el README.
3. **README** con contrato de integración, prueba de 8 minutos con contacto de prueba (`modo_prueba: true` de serie: esperas en minutos) y el interruptor único a producción.

Probado en local contra payloads reales: 6 cuellos, cuello por etiqueta `cuello-<x>`, acentos, sin cuello, con/sin WhatsApp, y la tarea puente con respuesta literal. Validadas las conexiones de ambos JSON.

## Qué está vivo y qué falta

- **Vivo:** nada aún en n8n (ver bloqueo abajo). Código y contrato, completos y probados en seco.
- **Falta de otros:** etapa "Tibio" y etapa/workflow de entrada (Landing) — sus IDs van en el nodo Config; copies finales de Ventas en `ventas/radiografia/` (hoy uso el borrador del cerebro §2; el cambio es 1 nodo); webhooks del agente de WhatsApp de Outbound llamando a `/radiografia/parada` cuando entre reply de WA.
- **Riesgo de duplicado [PARA CEREBRO]:** el reparto §4 asigna a Landing "emails día 1/3/7 por cuello" y tu encargo me los asigna a mí en n8n. Los he montado yo; que Landing NO los configure también en su workflow de GHL o el lead los recibirá dos veces. Deslindar antes de producción.

## Lo que necesito de Maikel (una sola lista, 10 minutos)

1. URL de su instancia n8n + API key (o que importe él los 2 JSON — están en mi rama).
2. Token API de GHL (subcuenta Qualivo) → una credencial Header Auth en n8n, compartida por todos los nodos.
3. Cuatro IDs de GHL para los nodos Config: `location_id`, `tibio_stage_id` (cuando Landing cree la etapa), `sdr_user_id`, `maikel_user_id`.
4. Un contacto de prueba suyo y 10 minutos para el curl de prueba del README (día 1/3/7 en ~8 min + parada + tarea puente).

## Cifras del turno

- Workflows entregados: **2** (secuencia + parada), 37 nodos, probados en seco.
- Coste de herramienta: **0 €** (n8n existente; modo caja).
- Horas ahorradas: las de perseguir a mano cada radiografía completada — con el objetivo de septiembre (30 conversaciones), la secuencia cubre los toques día 1/3/7 de cada lead sin tocar el bloque comercial de Maikel; el ahorro real se mide cuando haya volumen de radiografías.
- Bloqueo único: acceso a n8n (punto 1 de la lista).
