[project]
name = "tibia-pilot"
version = "0.1.0"
description = "Agente de Tibia por imitacion, sobre OTServer propio"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
api = ["fastapi>=0.110", "uvicorn>=0.27"]
otclient = ["websockets>=12.0"]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
